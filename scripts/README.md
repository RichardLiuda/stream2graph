# Scripts Directory

This directory contains developer and deployment helper scripts for starting services, checking local status, and packaging runtime artifacts.

Scripts should keep side effects explicit and document any required environment variables near the entry point.
