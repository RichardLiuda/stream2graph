# Configuration Directory

This directory keeps example and release-oriented configuration files for evaluation, fine-tuning, dataset preparation, and runtime experiments.

Local secrets and machine-specific overrides should stay out of the repository.
